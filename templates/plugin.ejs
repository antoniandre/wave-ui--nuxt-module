import Vue from 'vue';
<% if (options.useScss) { %>
import WaveUI from 'wave-ui/src/wave-ui/index.js';
<% } else { %>
import WaveUI from 'wave-ui';
<% } %>

Vue.use(WaveUI);

export default new WaveUI(<%= JSON.stringify(options.WaveUI, null, 2) %>);
